package edu.yu.introtoalgs;

/** Defines the API for the WellFormedString assignment: see the requirements
 * document for more information.
 *
 * Students MAY NOT change the constructor signature.  My test code will only
 * invoke the API defined below.
 *
 * @author Avraham Leff
 */
public class WellFormedString {
    public WellFormedString() {
	// fill in to taste
    }

    /** Returns true iff s is a well-formed string by the rules discussed in the
     * requirements doc, false otherwise.
     *
     * @param s the String to be evaluated for well-formedness
     * @return true iff s is well-formed, false otherwise.
     *
     * Note: to avoid religious wars, I commit to not test with empty or null
     * parameters.
     */
    public boolean isIt(final String s) {
	// supply a better implementation!
	return false;
    }  
}
